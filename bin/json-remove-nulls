#!/usr/bin/env jq-sh


__JQ__


include "walk_pre16";

walk_pre16(
  if type == "object" then
    delpaths([
      path(
        .[] |
        select(
          . == null or
          . == "null"
        )
      )
    ])
  else
    .
  end
)
